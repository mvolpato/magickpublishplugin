import XCTest
@testable import MagickPublishPlugin

final class MagickPublishPluginTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MagickPublishPlugin().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
